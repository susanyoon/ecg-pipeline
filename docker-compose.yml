services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ecg
      POSTGRES_PASSWORD: ecg_password
      POSTGRES_DB: ecg
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: